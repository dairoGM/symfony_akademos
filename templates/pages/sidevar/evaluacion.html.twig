<nav class="mt-2">
    <ul class="nav nav-pills nav-sidebar flex-column nav-legacy nav-child-indent" data-widget="treeview"
        role="menu"
        data-accordion="false">
        <!-- Add icons to the links using the .nav-icon class
             with font-awesome or any other icon font library -->


        {% if hasRoles('ROLE_GEST_SOLICITUD') or hasRoles('ROLE_GEST_INFORME_AUTOEVALUACION') or hasRoles('ROLE_GEST_PLAN_TRABAJO') or hasRoles('ROLE_GEST_PLAN_ANUAL_EVALUACION') or hasRoles('ROLE_GEST_APLAZAMIENTO') %}
            <li class="nav-item">
                <a href="#"
                   class="nav-link nav-link-menu">
                    <i class="nav-icon fa fa-pen"></i>
                    <p>
                        Evaluación
                        <i class="right fas fa-angle-left"></i>
                    </p>
                </a>
                <ul class="nav nav-treeview">
                    {% if hasRoles('ROLE_GEST_SOLICITUD') %}
                        <li class="nav-item">
                            <a href="{{ path('app_solicitud_index') }}"
                               class="nav-link nav-link-menu">
                                <p>Solicitudes</p>
                            </a>
                        </li>
                    {% endif %}

                    {% if hasRoles('ROLE_GEST_PLAN_ANUAL_EVALUACION') %}
                        <li class="nav-item">
                            <a href="{{ path('app_plan_anual_evaluacion_index') }}"
                               class="nav-link nav-link-menu">
                                <p>Plan anual de evaluación</p>
                            </a>
                        </li>
                    {% endif %}
                    {% if hasRoles('ROLE_GEST_INFORME_AUTOEVALUACION') %}
                        <li class="nav-item">
                            <a href="{{ path('app_informe_autoevaluacion_index') }}"
                               class="nav-link nav-link-menu">
                                <p>Informes de autoevaluación</p>
                            </a>
                        </li>
                    {% endif %}
                    {% if hasRoles('ROLE_GEST_PLAN_TRABAJO') %}
                        <li class="nav-item">
                            <a href="{{ path('app_plan_trabajo_index') }}"
                               class="nav-link nav-link-menu">
                                <p>Planes de trabajo</p>
                            </a>
                        </li>
                    {% endif %}
                    {% if hasRoles('ROLE_GEST_APLAZAMIENTO') %}
                        <li class="nav-item">
                            <a href="{{ path('app_aplazamiento_index') }}"
                               class="nav-link nav-link-menu">
                                <p>Solicitudes aplazadas</p>
                            </a>
                        </li>
                    {% endif %}


                </ul>
            </li>
        {% endif %}
        {% if hasRoles('ROLE_EVALAUCION_GEST_CATEGORIA_ACREDITACION') %}
            <li class="nav-item">
                <a href="#"
                   class="nav-link nav-link-menu">
                    <i class="nav-icon fas fa-cog"></i>
                    <p>
                        Acreditación
                        <i class="right fas fa-angle-left"></i>
                    </p>
                </a>
                <ul class="nav nav-treeview">

                    {% if hasRoles('ROLE_EVALAUCION_GEST_CATEGORIA_ACREDITACION') %}
                        <li class="nav-item">
                            <a href="{{ path('app_evaluacion_categoria_acreditacion_ies_index') }}"
                               class="nav-link nav-link-menu">
                                <p>Instituciones</p>
                            </a>
                        </li>
                    {% endif %}
                    {% if hasRoles('ROLE_EVALAUCION_GEST_CATEGORIA_ACREDITACION_PREGRADO') %}
                        <li class="nav-item">
                            <a href="{{ path('app_evaluacion_categoria_acreditacion_pregrado_index') }}"
                               class="nav-link nav-link-menu">
                                <p>Programas de pregrado</p>
                            </a>
                        </li>
                    {% endif %}
                    {% if hasRoles('ROLE_EVALAUCION_GEST_CATEGORIA_ACREDITACION_POSGRADO') %}
                        <li class="nav-item">
                            <a href="{{ path('app_evaluacion_categoria_acreditacion_posgrado_index') }}"
                               class="nav-link nav-link-menu">
                                <p>Programas de posgrado</p>
                            </a>
                        </li>
                    {% endif %}
                </ul>
            </li>
        {% endif %}
        {% if hasRoles('ROLE_GEST_CONVOCATORIA') or hasRoles('ROLE_GEST_ESTADO_SOLICITUD') or hasRoles('ROLE_GEST_ROL_COMISION') or hasRoles('ROLE_GEST_ESTADO_APLAZAMIENTO') %}
            <li class="nav-item">
                <a href="#"
                   class="nav-link nav-link-menu">
                    <i class="nav-icon fas fa-cog"></i>
                    <p>
                        Configuración
                        <i class="right fas fa-angle-left"></i>
                    </p>
                </a>
                <ul class="nav nav-treeview">

                    {% if hasRoles('ROLE_GEST_CONVOCATORIA') %}
                        <li class="nav-item">
                            <a href="{{ path('app_convocatoria_index') }}"
                               class="nav-link nav-link-menu">
                                <p>Convocatorias</p>
                            </a>
                        </li>
                    {% endif %}
                    {% if hasRoles('ROLE_GEST_ESTADO_SOLICITUD') %}
                        <li class="nav-item">
                            <a href="{{ path('app_estado_solicitud_index') }}"
                               class="nav-link nav-link-menu">
                                <p>Estados de solicitud</p>
                            </a>
                        </li>
                    {% endif %}
                    {% if hasRoles('ROLE_GEST_ESTADO_APLAZAMIENTO') %}
                        <li class="nav-item">
                            <a href="{{ path('app_estado_aplazamiento_index') }}"
                               class="nav-link nav-link-menu">
                                <p>Estados de aplazamiento</p>
                            </a>
                        </li>
                    {% endif %}
                    {% if hasRoles('ROLE_GEST_ROL_COMISION') %}
                        <li class="nav-item">
                            <a href="{{ path('app_evaluacion_rol_comision_index') }}"
                               class="nav-link nav-link-menu">
                                <p>Roles</p>
                            </a>
                        </li>
                    {% endif %}

                    {% if hasRoles('ROLE_GEST_COMISION_EVALUADORA') %}
                        <li class="nav-item">
                            <a href="{{ path('app_comision_evaluadora_index') }}"
                               class="nav-link nav-link-menu">
                                <p>Comisión evaluadora</p>
                            </a>
                        </li>
                    {% endif %}
                </ul>
            </li>
        {% endif %}

    </ul>
</nav>