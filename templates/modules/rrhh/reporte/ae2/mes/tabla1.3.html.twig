<div class="table-responsive">
    <table class="table table-striped table-bordered">
        <thead >
        <tr>
            <th>CATEGORÍA</th>
            <th>CANTIDAD</th>
            <th>PORCENTAJE</th>
        </tr>
        </thead>
        <tbody>
        {% for item in datos %}
            <tr>
                <td>{{ item.categoria }}</td>
                <td class="text-center">{{ item.cantidad|number_format(0, ',', '.') }}</td>
                <td class="text-center">{{ item.porcentaje|number_format(1, ',', '.') }}%</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>
</div>